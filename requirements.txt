# CI 与本地环境的依赖。版本钉死——akshare 更新很频繁，
# 它一旦改了接口解析，抓取会在无人值守时静默失效。
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# 升级 akshare 前先在本地跑一遍 probe_reachability.py --akshare 和
# update_data.py，确认字段口径没变再改这里。

akshare==1.18.65
anthropic>=0.69.0

# akshare 的传递依赖，一并钉死以保证 CI 可复现
pandas==3.0.3
numpy==2.4.6
requests==2.34.2
lxml==6.1.1
